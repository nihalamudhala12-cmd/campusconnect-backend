// Notifications Module Index
// M12 — Notifications API
//
// Registers the notifications module router with the module system.

const notificationsRoutes = require('./routes');

module.exports = notificationsRoutes;
