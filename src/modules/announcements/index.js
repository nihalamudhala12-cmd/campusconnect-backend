/**
 * Announcements Module Index
 * M8 — Announcements API
 *
 * Exports the announcements module router for registration in the main route registry.
 */

const announcementRoutes = require('./routes');

module.exports = announcementRoutes;