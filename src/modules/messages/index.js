/**
 * Messages Module
 * M9 — Messages API
 *
 * Exports the messages module router for registration in the main route registry.
 */

const messagesRoutes = require('./routes');

module.exports = messagesRoutes;
