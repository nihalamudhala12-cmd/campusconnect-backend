// Devices Module Index
// M13 — Devices API
//
// Registers the devices module router for registration in the main route registry.

const devicesRoutes = require('./devices/routes');

module.exports = devicesRoutes;
